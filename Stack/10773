import sys
input = sys.stdin.readline

n = int(input())

stack = []
for _ in range(n) :
    num = int(input())
    if num == 0 :
        stack.pop()
    else :
        stack.append(num)

total = 0
for j in stack :
    total += j

print(total)
